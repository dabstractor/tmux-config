# Tubular TMux Configuration
# Define your colors and content - the plugin handles all styling

# absolute-centre keeps the window list dead-center regardless of the
# left/right section widths (this replaces the old shell-based centering)
set -g status-justify "absolute-centre"

# Window alerts: light up a backgrounded window's tab when it produces output
# (activity) or rings the bell (urgent). tubular styles them — activity gets a
# brighter foreground, a bell gets a red tab. See README "Window alerts".
setw -g monitor-activity on
setw -g monitor-bell on

# === Prefix Key (Required) ===
# Must match the `prefix` you set in tmux — the plugin takes this key over.
set -g @tubular_prefix_key "C-Space"

# === Color Configuration ===
# Background colors
set -g @tubular_bg "#1f1f28"              # Main background
set -g @tubular_bg_max "#181822"          # Darker background (assuming dark theme)
set -g @tubular_bg_min "#24242e"          # Lighter background (assuming dark theme)

# Foreground colors
set -g @tubular_fg "#c7c7aa"              # Main text
set -g @tubular_fg_active "#cde4ed"       # Active text
set -g @tubular_fg_focus "#dddddd"        # Focused text
set -g @tubular_neutral_visible "#787878" # Subdued UI elements
set -g @tubular_neutral_hidden "#54546d"  # Faint indicators

# Mode-specific colors (the only colors that matter)
set -g @tubular_zoom_color "#e6c384"      # Zoom mode background
set -g @tubular_copy_color "#98bb6c"      # Copy mode background
set -g @tubular_prefix_color "#d27e99"    # Prefix mode background
set -g @tubular_active_color "#7aa89f"    # Active pane border

# === Content Configuration ===
# This config is "bucket C" (my own text, tubular's colors): every slot whose
# @tubular_*_text is set is rendered by tubular; slots you don't set fall back
# to your native tmux value. No @tubular_manage_content switch is needed here
# because all three slots are set explicitly.
#
# Theme colors use {{token}} shortcuts (expanded once at load; see the README's
# Theme Color Reference). Dynamic ones like {{mode_fg}} inject the required
# #{E:...} for you, so you never manage the E: distinction by hand.
set -g @tubular_window_tab_text " #W "
set -g @tubular_status_left_text "#{?client_prefix,󰞦#[fg={{copy}}]󰞦#[fg={{mode_fg}}]󰞦, #{?window_zoomed_flag,{{zoom_indicator}},#[fg=#{?pane_in_mode,{{mode_fg}},{{copy}}}]󰞦 #[fg={{mode_fg}}]}} #S  "
set -g @tubular_status_right_text "#{?window_zoomed_flag,{{zoom_indicator}}#{window_panes}  , }#{b:pane_current_path}  󰃰  %I:%M  "

set -g @tubular_tab_start ""
set -g @tubular_tab_end ""
set -g @tubular_separator "   "
# set -g @tubular_zoom_indicator " "

# === Border Style Configuration ===
# Note: pane-border-lines is not a format option in tmux, so the line style
# (heavy/double/...) is fixed; only border colors/bold change per mode.
set -g @tubular_normal_border_lines "heavy"
# set -g @tubular_normal_border_lines "single"
set -g @tubular_normal_extra_bold "0"
set -g @tubular_active_extra_bold "1"
set -g @tubular_prefix_extra_bold "1"
set -g @tubular_copy_extra_bold "0"


# === Mode Foreground Colors (Optional) ===
# set -g @tubular_prefix_status_fg "#dcdcec"
# set -g @tubular_zoom_status_fg "#000000"
# set -g @tubular_copy_status_fg "#e6c384"


# === Color Palette ===
# # Background colors
# thm_bg="#1f1f28"        # sumiInk1 - main background
# thm_black="#16161d"     # sumiInk0 - darker background
# thm_gray="#2a2a37"      # sumiInk4 - UI elements background
# thm_black4="#54546d"    # sumiInk6 - darker UI elements
#
# # Foreground colors
# thm_fg="#dcd7ba"        # fujiWhite - main text
# thm_cyan="#7aa89f"      # waveAqua2 - cyan accent
# thm_blue="#7e9cd8"      # crystalBlue - blue accent
# thm_magenta="#957fb8"   # oniViolet - purple accent
# thm_pink="#d27e99"      # sakuraPink - pink accent
# thm_red="#e82424"       # samuraiRed - red/error
# thm_orange="#ffa066"    # surimiOrange - orange accent
# thm_yellow="#e6c384"    # boatYellow2 - yellow accent
# thm_green="#98bb6c"     # springGreen - green accent
#
# # Background colors
# color_bg=$thm_bg        # Main background
# color_bg_dark="#181822"     # Darker backgrounds
# color_bg_light="#24242e"    # Lighter backgrounds
# # color_gray="#2A2A37"      # UI element backgrounds
# color_gray=$thm_gray      # UI element backgrounds
# color_black=$thm_black     # Darker backgrounds
# color_dark_gray="#54546D" # Darker UI elements
# color_light_gray="#787878"
# color_white="#cccccc"
# # color_soft_white="#CDD6F4"
#
# # Foreground/text colors
# color_fg=$thm_fg        # Main text
#
# # Accent colors
# color_orange="#FFA066"    # Active tab highlight
# color_blue="#7E9CD8"      # Inactive tab highlight
# color_cyan="#7AA89F"
# color_magenta="#957FB8"
# # color_pink=$thm_pink
# color_pink="#d27e99"
# # color_pink="#EEAAAA"
# # color_red="#E82424"
# color_red=$thm_red
# # color_yellow="#E6C384"
# color_yellow=$thm_yellow
# color_green=$thm_green
#

set -g @plugin 'dabstractor/tubular-tmux'
